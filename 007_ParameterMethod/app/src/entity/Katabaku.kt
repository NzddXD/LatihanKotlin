package app.src.entity

class Katabaku {
    // Attr here -------------------------------------

    // Method here -----------------------------------
    fun add(v1 : Int, v2 : Int) = (v1 + v2)
    fun time(v1 : Int, v2 : Int) = (v1 * v2)
    fun subtract(v1 : Int, v2 : Int) = (v1 - v2)
    fun divide(v1 : Int, v2 : Int) = (v1 / v2)
}